# Grafana Dashboards

- `cluster-overview.json` — EKS cluster health
- `tenant-pods.json` — Per-tenant Agent Pod metrics
- `cost-per-tenant.json` — Token usage and estimated cost per tenant
- `billing-overview.json` — Revenue and usage trends
