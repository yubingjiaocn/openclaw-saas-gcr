apiVersion: v1
kind: Service
metadata:
  name: platform-api
  namespace: openclaw-platform
  labels:
    app: platform-api
  annotations:
    # AWS Load Balancer Controller v2.4+: "external" ensures the controller
    # manages this NLB (not the in-tree cloud provider). The legacy "nlb"
    # value may fall through to in-tree, losing SG/target-group management.
    service.beta.kubernetes.io/aws-load-balancer-type: "external"
    service.beta.kubernetes.io/aws-load-balancer-scheme: "internet-facing"
    service.beta.kubernetes.io/aws-load-balancer-nlb-target-type: "ip"
    service.beta.kubernetes.io/aws-load-balancer-attributes: "load_balancing.cross_zone.enabled=true"
    # Security: restrict NLB inbound to CloudFront prefix list.
    # LB Controller creates a managed SG with these prefix lists as inbound rules.
    # Set to empty to allow all (e.g. workshop/dev). Override via envsubst.
    service.beta.kubernetes.io/aws-load-balancer-security-group-prefix-lists: "${NLB_SG_PREFIX_LISTS}"
    # Health check
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-path: "/health"
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-protocol: "HTTP"
spec:
  type: LoadBalancer
  loadBalancerClass: service.k8s.aws/nlb
  selector:
    app: platform-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
