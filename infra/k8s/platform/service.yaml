apiVersion: v1
kind: Service
metadata:
  name: platform-api
  namespace: openclaw-platform
  labels:
    app: platform-api
spec:
  type: ClusterIP
  selector:
    app: platform-api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
